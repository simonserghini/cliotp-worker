{
  "name": "cliotp-worker",
  "version": "0.1.0",
  "description": "Self-hosted TOTP/HOTP/Steam server on Cloudflare Workers + Durable Objects.",
  "type": "module",
  "scripts": {
    "test": "node --test",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  },
  "license": "MIT"
}
